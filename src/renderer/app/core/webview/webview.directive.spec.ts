import { WebviewDirective } from './webview.directive';

describe('WebviewDirective', () => {
  let directive: WebviewDirective;

  beforeEach(() => {
    directive = new WebviewDirective();
  });

  it('should create', () => {
    expect(directive).toBeTruthy();
  });
});
